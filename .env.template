# QuickQuery AI Student Helpdesk - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# ENVIRONMENT SETTINGS
# =============================================================================
ENVIRONMENT=development
# Options: development, production, testing

DEBUG=true
# Enable debug mode (true/false)

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
APP_TITLE=QuickQuery - AI Student Helpdesk
APP_DESCRIPTION=Enhanced AI-powered student assistance platform
APP_VERSION=2.0.0
HOST=0.0.0.0
PORT=8501

# Feature flags
ENABLE_ANALYTICS=true
ENABLE_FEEDBACK=true
MAX_CHAT_HISTORY=50
CACHE_TTL=3600

# =============================================================================
# OPENAI API CONFIGURATION
# =============================================================================
OPENAI_API_KEY=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
# Get your API key from: https://platform.openai.com/api-keys

OPENAI_MODEL=gpt-3.5-turbo
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview

OPENAI_MAX_TOKENS=500
# Maximum tokens per response

OPENAI_TEMPERATURE=0.7
# Response creativity (0.0 - 2.0)

OPENAI_TIMEOUT=30
# Request timeout in seconds

OPENAI_MAX_RETRIES=3
# Maximum retry attempts

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
SECRET_KEY=your_secret_key_here_generate_a_strong_random_key
# Generate using: python -c "import secrets; print(secrets.token_hex(32))"

SESSION_TIMEOUT=3600
# Session timeout in seconds (1 hour)

MAX_REQUEST_SIZE=1048576
# Maximum request size in bytes (1MB)

RATE_LIMIT_PER_MINUTE=60
# Rate limiting per minute per user

ALLOWED_ORIGINS=http://localhost:8501,https://*.streamlit.app
# Comma-separated list of allowed CORS origins

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

LOG_TO_FILE=false
# Enable logging to file (true/false)

LOG_DIR=logs
# Directory for log files

LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
# Log message format

LOG_MAX_FILE_SIZE=10485760
# Maximum log file size in bytes (10MB)

LOG_BACKUP_COUNT=5
# Number of backup log files to keep

# =============================================================================
# DATABASE CONFIGURATION (Optional - for future features)
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quickquery
DB_USER=quickquery_user
DB_PASSWORD=your_database_password_here
DB_SSL_MODE=prefer

# =============================================================================
# MONITORING AND ANALYTICS (Optional)
# =============================================================================
ENABLE_MONITORING=false
MONITORING_ENDPOINT=
ANALYTICS_TRACKING_ID=

# =============================================================================
# DEPLOYMENT SETTINGS
# =============================================================================
# For production deployment
WORKERS=1
# Number of worker processes

WORKER_TIMEOUT=120
# Worker timeout in seconds

PRELOAD_APP=true
# Preload application for better performance

# =============================================================================
# THIRD-PARTY INTEGRATIONS (Optional)
# =============================================================================
# Email configuration for notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_USE_TLS=true

# Redis configuration for caching (optional)
REDIS_URL=redis://localhost:6379/0

# External API endpoints
UNIVERSITY_API_ENDPOINT=
UNIVERSITY_API_KEY=

# =============================================================================
# SECURITY HEADERS AND POLICIES
# =============================================================================
# Content Security Policy
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self' 'unsafe-inline' 'unsafe-eval'
CSP_STYLE_SRC='self' 'unsafe-inline'
CSP_IMG_SRC='self' data: https:

# Additional security headers
X_FRAME_OPTIONS=DENY
X_CONTENT_TYPE_OPTIONS=nosniff
X_XSS_PROTECTION=1; mode=block
REFERRER_POLICY=strict-origin-when-cross-origin

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Only for development environment
DEV_RELOAD=true
DEV_LOG_LEVEL=DEBUG
DEV_ENABLE_PROFILING=false

# =============================================================================
# TESTING SETTINGS
# =============================================================================
# Only for testing environment
TEST_DATABASE_URL=sqlite:///test.db
TEST_SKIP_AUTH=true
TEST_MOCK_OPENAI=true